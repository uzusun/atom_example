FROM centos:centos7
#RUN echo 'trouble' > /tmp/trouble.txt
#RUN echo 'shoot' >> /tmp/shoot.txt

#MAINTAINER uzusun <ektkfdl@naver.com>
### ENV 유의 사항 ###
# 여러 개의 환경 변수를 정의할 때 하나의 인스트럭션을 표횬
# 환경 변수는 이미지에서 컨테이너를 생성할 때 사용할 수 있다
# docker inspect <이미지> 정의된 환경 변수의 값을 확인할수있다
# docker run 시 --env 키=값 옵션을 지정하면 환경 변수의 값을 변경 할 수 있다

## ADD 유의 사항 ###
# 컨테이너 이미지에 복사된 파일과 디렉터리의 UID와 GID는 모두 0이다
# 원격 URL이라면 목적지에 복사된 파일의 접근 권한은 600
# 소스 파일은 모두 빌드 컨텍스트 디렉터리나 그 하위에 있어야한다
# 로컬 소스 파일이 tar 아카이브로 돼 있다면, 이를 풀어서 디렉터리 형태로 만든다
# 소스 파일을 여러 개 지정할 때는 목적지가 반드시 디렉터리여야 하며 경로 끝에 반드시 슬래시(/)를 붙여야한다
# 목적지가 존재하지 않는다면 새로 생성. 경로에 나온 모든 부모 디렉터리도 똑같이 적용


## CMD 와 RUN 와 다른점
# RUN은 이미지를 빌드하는 동안 실제로 명령을 실행한 후 결과를 커밋해서 레이어를 추가
# CMD는 빌드하는 동안에는 실행되지 않고 컨테이너에서 실행할 이미지를 지정하기만 한다
